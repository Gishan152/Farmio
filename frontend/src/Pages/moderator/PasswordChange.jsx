import PasswordChangeForm from '../../components/auth/PasswordChangeForm';

const ModeratorPasswordChange = () => {
  return <PasswordChangeForm />;
};

export default ModeratorPasswordChange;
