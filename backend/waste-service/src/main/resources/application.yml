# Server
server:
  port: 8088

# Database
spring:
  application:
    name: waste-service
  cloud:
#    gateway:
#      routes:
#        - id: waste-service
#          uri: lb://waste-service
#          predicates:
#            - Path=/api/waste/**
    config:
      enabled: false
  rabbitmq:
    host: ${RABBITMQ_HOST:localhost}
    port: ${RABBITMQ_PORT:5672}

  datasource:
    url: jdbc:postgresql://ep-dry-snow-a1szex24-pooler.ap-southeast-1.aws.neon.tech/wastedb?sslmode=require&channelBinding=require
    username: neondb_owner
    password: npg_l2wurAbES8Cq
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
#management:
#  endpoints:
#    web:
#      exposure:
#        include: "*"
#    health:
#      show-components: always
#      show-details: always
#      db:
#        enabled: true
#  security:
#    enabled: false

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka}

