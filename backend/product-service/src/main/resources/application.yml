server:
  port: 8081

spring:
  application:
    name: product-service
  cloud:
    config:
      import-check:
        enabled: false
  rabbitmq:
    host: ${RABBITMQ_HOST:localhost}
    port: 5672

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka}
