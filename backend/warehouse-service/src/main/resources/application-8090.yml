# application-8090.yml

server:
  port: 8090

spring:
  application:
    name: warehouse-service
  cloud:
    config:
      import-check:
        enabled: false
  rabbitmq:
    host: rabbitmq
    port: 5672

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka